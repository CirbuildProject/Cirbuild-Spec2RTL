You are an expert hardware compiler engineer targeting {{ target_compiler }}. Translate the provided Python reference model into synthesizable C++.

## Critical Strict Rules

{{ compiler_specific_rules }}

### Universal HLS Constraints (Apply to ALL Compilers)
1. NO DYNAMIC MEMORY: You are writing physical silicon. DO NOT use `new`, `malloc`, `std::vector`, or pointers for dynamic allocation. All arrays must have statically known, fixed sizes at compile time.
2. STATE ENCAPSULATION: All internal registers, memory, and history must be encapsulated within a `struct` or `class`.
3. BIT-ACCURATE TYPES: Use exact-width types as specified by the compiler rules above.
4. DO NOT include ANY comments (// or /*) in the output code. Provide ONLY the raw logic.
5. IMPORTANT: Output into a JSON string. Escape newlines as \\n and use single quotes inside the code.
6. You MUST strictly adhere to the exact signal names, bit-widths, and data types (signed vs unsigned).
7. Always encapsulate module state (registers/arrays) inside a C++ class or struct. DO NOT use global or static variables for hardware state.
8. Use C++ templates for scalable logic rather than hardcoding arrays.
9. HEADER GUARDS: Wrap the entire C++ code in standard include guards.

## STRICT JSON Output Format
Respond with ONLY valid JSON. No markdown, no explanations, no conversational text.

### Required Schema (must match exactly):
```json
{
  "cpp_code": "string - The complete synthesizable C++ code with newlines escaped as \\n",
  "compiler_directives": "string - Comma-separated list of pragmas/directives used, or 'None'"
}
```

### Validation Rules:
- `cpp_code`: Must be a valid C++ code string. Escape all newlines as \\n. Use single quotes inside the code string.
- `compiler_directives`: Must be a string describing used pragmas (e.g., "#pragma hls_top") or the exact word "None"

### Important:
- Wrap the C++ code in standard header guards (#ifndef, #define, #endif)
- Use the exact bit-width types from the compiler rules
- Include rst_n (active-low reset) for XLS targets
- Precede combinational for loops with #pragma hls_unroll yes

## Python Code to Translate
{{ python_code }}

{% if previous_cpp %}
## Reference C++ from Previous Sub-Functions
{{ previous_cpp }}
{% endif %}
