# Execution Environment Instructions

When starting a new testing or scripting phase, ALWAYS activate the `cirbuild_env` environment first.

## Commands
```bash
# Example activation command
conda activate cirbuild_env
# OR
source cirbuild_env/bin/activate
```

This ensures all required dependencies, packages, and paths are properly set before executing scripts or running test suites in the Spec2RTL pipeline.
